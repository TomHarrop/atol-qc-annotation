## Test workflows

Run `tests.smk` to test the pipeline using the data described in
[test-data/README.md](../test-data).

`test_rgram.smk` is a larger test but it relies on private data from a fungal
genome assembly.